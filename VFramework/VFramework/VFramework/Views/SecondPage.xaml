<?xml version="1.0" encoding="utf-8" ?>
<views:CustomContentPage x:TypeArguments="viewModels:SecondViewModel" xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:VFramework.ViewModels;assembly=VFramework"
             xmlns:views="clr-namespace:VFramework.Views;assembly=VFramework"
             x:Class="VFramework.Views.SecondPage">
  <StackLayout>
    <Label Text="{Binding MainText}" VerticalOptions="Center" HorizontalOptions="Center" />
    <Button Text="Back up, back up Tell me what you're gonna do now" Command="{Binding GoBackCommand}" />
    <Button Text="Go to modal" Command="{Binding GoModalCommand}" />
  </StackLayout>
</views:CustomContentPage>